{
  "planPath": "docs/superpowers/plans/2026-08-15-utilities-self-governing-lint.md",
  "specPath": "docs/superpowers/specs/2026-08-14-utilities-self-governing-lint-design.md",
  "tasks": [
    {
      "id": 9,
      "subject": "Task 1: Configuration and environment",
      "status": "pending",
      "description": "**Goal:** `utilities/` carries its own ruff/mypy/pytest config and a reproducible pixi environment, reporting the same 33 findings the outer scaffold did.\n\n**Files:**\n- Create: `/workspace/utilities/pyproject.toml`\n- Create: `/workspace/utilities/pixi.toml`\n- Create: `/workspace/utilities/pixi.lock` (generated by `pixi install`)\n- Modify: `/workspace/utilities/.gitignore`\n\n**Acceptance Criteria:**\n- [ ] `pixi install` succeeds and writes `pixi.lock`\n- [ ] `pixi.lock` contains solved packages for `linux-64`, `osx-arm64` and `osx-64`\n- [ ] `pixi run lint` reports exactly `Found 33 errors.`\n- [ ] `pixi run python -m ruff check test_printall.py` reports `All checks passed!`, proving the `test_*.py` per-file-ignore pattern actually matches\n- [ ] `pixi run typecheck` reports `Success: no issues found in 16 source files`\n- [ ] `pixi run test` reports `35 passed`\n- [ ] `.pixi/` is gitignored and `pixi.lock` is committed\n\n**Verify:** `cd /workspace/utilities && pixi run lint 2>&1 | tail -2` → `Found 33 errors.`\n\nFull file contents and the macOS-solve fallback are in Task 1 of the plan.\n\n```json:metadata\n{\"files\": [\"pyproject.toml\", \"pixi.toml\", \"pixi.lock\", \".gitignore\"], \"verifyCommand\": \"cd /workspace/utilities && pixi run lint 2>&1 | tail -2\", \"acceptanceCriteria\": [\"pixi install succeeds and writes pixi.lock\", \"pixi.lock covers linux-64, osx-arm64 and osx-64\", \"pixi run lint reports exactly Found 33 errors.\", \"pixi run python -m ruff check test_printall.py reports All checks passed!\", \"pixi run typecheck reports Success: no issues found in 16 source files\", \"pixi run test reports 35 passed\", \".pixi/ gitignored and pixi.lock committed\"], \"modelTier\": \"standard\"}\n```"
    },
    {
      "id": 10,
      "subject": "Task 2: Pre-commit config and hook install",
      "status": "pending",
      "blockedBy": [9],
      "description": "**Goal:** Commits into `utilities/` are gated by hooks defined in `utilities/` itself, for the first time.\n\n**Files:**\n- Create: `/workspace/utilities/.pre-commit-config.yaml`\n\n**Acceptance Criteria:**\n- [ ] `.git/hooks/pre-commit` exists after `pixi run pre-commit-install`\n- [ ] The commit for this task passes the whole-repo `mypy` hook\n- [ ] `pixi run pre-commit run --files pyproject.toml` shows `check-toml` `Passed`\n\n**Verify:** `cd /workspace/utilities && ls .git/hooks/pre-commit && pixi run pre-commit run --files pyproject.toml 2>&1 | rg 'check-toml|mypy'` → both `Passed`\n\nSix local hooks, no remote repos, all routed through `pixi run python -m <tool>`. Full YAML in Task 2 of the plan. The staged file is YAML so the ruff hooks skip it; only whole-repo mypy runs, and it is already green.\n\n```json:metadata\n{\"files\": [\".pre-commit-config.yaml\"], \"verifyCommand\": \"cd /workspace/utilities && ls .git/hooks/pre-commit && pixi run pre-commit run --files pyproject.toml 2>&1 | rg 'check-toml|mypy'\", \"acceptanceCriteria\": [\".git/hooks/pre-commit exists\", \"commit passes the whole-repo mypy hook\", \"check-toml reports Passed\"], \"modelTier\": \"mechanical\"}\n```"
    },
    {
      "id": 11,
      "subject": "Task 3: Clear download_file.py",
      "status": "pending",
      "blockedBy": [10],
      "description": "**Goal:** Remove the 28 findings in `download_file.py` — 18 dead imports, both `UP035`, one `UP039`, four `D212` and two dead asserts.\n\n**Files:**\n- Modify: `/workspace/utilities/download_file.py`\n\n**Acceptance Criteria:**\n- [ ] `pixi run python -m ruff check download_file.py` → `All checks passed!`\n- [ ] `pixi run lint` → `Found 5 errors.`\n- [ ] `pixi run python download_file.py --help` exits 0\n- [ ] `pixi run typecheck` → `Success: no issues found in 16 source files`\n- [ ] No `assert` remains in the file\n\n**Verify:** `cd /workspace/utilities && pixi run lint 2>&1 | tail -2` → `Found 5 errors.`\n\nThe two `assert options.args is not None` statements are DELETED, not converted to raises: `self.args` is declared `argparse.Namespace`, never `Optional`. Exact before/after blocks for all four docstrings and the import replacement are in Task 3 of the plan. Match on quoted text, not line numbers — earlier edits shift later lines.\n\n```json:metadata\n{\"files\": [\"download_file.py\"], \"verifyCommand\": \"cd /workspace/utilities && pixi run lint 2>&1 | tail -2\", \"acceptanceCriteria\": [\"ruff check download_file.py reports All checks passed!\", \"pixi run lint reports Found 5 errors.\", \"python download_file.py --help exits 0\", \"pixi run typecheck reports Success: no issues found in 16 source files\", \"no assert remains in the file\"], \"modelTier\": \"mechanical\"}\n```"
    },
    {
      "id": 12,
      "subject": "Task 4: Clear check_internet.py",
      "status": "pending",
      "blockedBy": [11],
      "description": "**Goal:** Remove the 3 findings in `check_internet.py` — one import-order and two docstring-placement.\n\n**Files:**\n- Modify: `/workspace/utilities/check_internet.py`\n\n**Acceptance Criteria:**\n- [ ] `pixi run python -m ruff check check_internet.py` → `All checks passed!`\n- [ ] `pixi run lint` → `Found 2 errors.`\n- [ ] `pixi run python check_internet.py --help` exits 0\n\n**Verify:** `cd /workspace/utilities && pixi run lint 2>&1 | tail -2` → `Found 2 errors.`\n\nExact before/after blocks in Task 4 of the plan. Do not run the script without `--help` — it makes real network calls and sets its exit code from the result.\n\n```json:metadata\n{\"files\": [\"check_internet.py\"], \"verifyCommand\": \"cd /workspace/utilities && pixi run lint 2>&1 | tail -2\", \"acceptanceCriteria\": [\"ruff check check_internet.py reports All checks passed!\", \"pixi run lint reports Found 2 errors.\", \"python check_internet.py --help exits 0\"], \"modelTier\": \"mechanical\"}\n```"
    },
    {
      "id": 13,
      "subject": "Task 5: Demote test_parse_datetime.py to a demo",
      "status": "pending",
      "blockedBy": [12],
      "description": "**Goal:** Stop a file that asserts nothing from masquerading as a test, and clear its 2 findings.\n\n**Files:**\n- Rename: `/workspace/utilities/test_parse_datetime.py` → `/workspace/utilities/demo_parse_datetime.py`\n- Modify: `/workspace/utilities/demo_parse_datetime.py`\n\n**Acceptance Criteria:**\n- [ ] `test_parse_datetime.py` no longer exists; `demo_parse_datetime.py` does, with history preserved via `git mv`\n- [ ] `pixi run python -m ruff check demo_parse_datetime.py` → `All checks passed!`\n- [ ] `pixi run lint` → `All checks passed!` (0 findings, down from 33)\n- [ ] `pixi run test` still reports `35 passed`\n- [ ] The four repositioned comments are preserved verbatim, just moved above their statements\n- [ ] Lines 153-157's commented-out assertions are left untouched\n\n**Verify:** `cd /workspace/utilities && pixi run lint 2>&1 | tail -2 && pixi run test 2>&1 | tail -2` → `All checks passed!` and `35 passed`\n\n`35 passed` is the point of this task — it is the falsifiable form of the claim that the file was never a test. If the count moves off 35, stop and reconsider rather than adjusting the number. Exactly four lines (111, 117, 120, 129) need their trailing comment moved above; a bare `print(` on its own line after formatting means a fifth was missed. Full before/after blocks in Task 5 of the plan.\n\n```json:metadata\n{\"files\": [\"test_parse_datetime.py\", \"demo_parse_datetime.py\"], \"verifyCommand\": \"cd /workspace/utilities && pixi run lint 2>&1 | tail -2 && pixi run test 2>&1 | tail -2\", \"acceptanceCriteria\": [\"test_parse_datetime.py gone, demo_parse_datetime.py present, renamed via git mv\", \"ruff check demo_parse_datetime.py reports All checks passed!\", \"pixi run lint reports All checks passed!\", \"pixi run test still reports 35 passed\", \"the four repositioned comments are preserved verbatim\", \"commented-out assertions at lines 153-157 untouched\"], \"modelTier\": \"mechanical\"}\n```"
    },
    {
      "id": 14,
      "subject": "Task 6: Format the remaining file",
      "status": "pending",
      "blockedBy": [13],
      "description": "**Goal:** Every file in the repo is ruff-formatted, making `pre-commit run --all-files` reachable.\n\n**Files:**\n- Modify: `/workspace/utilities/detect_country.py`\n\n**Acceptance Criteria:**\n- [ ] `pixi run format` reports every file already formatted on a second run (idempotent)\n- [ ] `pixi run lint` → `All checks passed!`\n- [ ] `pixi run test` → `35 passed`\n- [ ] Exactly one file is reformatted; none of the five adopted scripts change\n\n**Verify:** `cd /workspace/utilities && pixi run format --check 2>&1 | tail -1` → `16 files already formatted`\n\nTasks 3, 4 and 5 each staged a file and the `ruff-format` hook formats staged files, so `detect_country.py` is the only unformatted file left. If more than one file is reformatted, an earlier task skipped its format step.\n\n```json:metadata\n{\"files\": [\"detect_country.py\"], \"verifyCommand\": \"cd /workspace/utilities && pixi run format --check 2>&1 | tail -1\", \"acceptanceCriteria\": [\"ruff format is idempotent on a second run\", \"pixi run lint reports All checks passed!\", \"pixi run test reports 35 passed\", \"exactly one file reformatted and no adopted script changes\"], \"modelTier\": \"mechanical\"}\n```"
    },
    {
      "id": 15,
      "subject": "Task 7: Add _template.py",
      "status": "pending",
      "blockedBy": [14],
      "description": "**Goal:** New scripts start from a clean skeleton, so the boilerplate debt just removed cannot regenerate.\n\n**Files:**\n- Create: `/workspace/utilities/_template.py`\n\n**Acceptance Criteria:**\n- [ ] `pixi run python -m ruff check _template.py` → `All checks passed!`\n- [ ] `pixi run typecheck` → `Success: no issues found in 17 source files`\n- [ ] `pixi run python _template.py --help` exits 0\n- [ ] `pixi run python _template.py` exits 0 and logs one line\n- [ ] `pixi run test` still reports `35 passed` — the leading underscore keeps pytest from collecting it\n- [ ] The file contains no `import emmykit`\n\n**Verify:** `cd /workspace/utilities && pixi run python _template.py --help > /dev/null && pixi run python -m ruff check _template.py` → exit 0 and `All checks passed!`\n\nThe emmykit line is a COMMENT, not an import: a speculative `import emmykit as ek` would be `F401`, making the template ship the exact defect it exists to prevent. Full file content in Task 7 of the plan.\n\n```json:metadata\n{\"files\": [\"_template.py\"], \"verifyCommand\": \"cd /workspace/utilities && pixi run python _template.py --help > /dev/null && pixi run python -m ruff check _template.py\", \"acceptanceCriteria\": [\"ruff check _template.py reports All checks passed!\", \"pixi run typecheck reports 17 source files\", \"python _template.py --help exits 0\", \"python _template.py exits 0 and logs one line\", \"pixi run test still reports 35 passed\", \"no import emmykit in the file\"], \"modelTier\": \"mechanical\"}\n```"
    },
    {
      "id": 16,
      "subject": "Task 8: Update the documentation",
      "status": "pending",
      "blockedBy": [15],
      "description": "**Goal:** `README.md` and `CLAUDE.md` describe the repo as it now is, and record the conventions that keep it clean.\n\n**Files:**\n- Modify: `/workspace/utilities/README.md`\n- Modify: `/workspace/utilities/CLAUDE.md`\n\n**Acceptance Criteria:**\n- [ ] `rg 'test_parse_datetime' -g '!docs/**' -g '!.git/**'` returns no matches\n- [ ] `README.md`'s structure table lists `_template.py`, `demo_parse_datetime.py` and `docs/`\n- [ ] `CLAUDE.md` documents running tools from this directory, the `_template.py` convention, and the `test_*` / `demo_*` naming rule\n- [ ] `pixi run pre-commit-all` → every hook `Passed`, none `Failed`\n- [ ] Final acceptance block passes: lint clean, 17 files formatted, mypy 17 files, 35 tests\n\n**Verify:** `cd /workspace/utilities && pixi run pre-commit-all 2>&1 | rg -c 'Passed'` → 6\n\n`pre-commit run --all-files` going green is the deliverable — it has never been green for this repository. Exact markdown blocks in Task 8 of the plan. Step 7 there is optional and touches `/workspace`, a different repository; do not commit it here.\n\n```json:metadata\n{\"files\": [\"README.md\", \"CLAUDE.md\"], \"verifyCommand\": \"cd /workspace/utilities && pixi run pre-commit-all 2>&1 | rg -c 'Passed'\", \"acceptanceCriteria\": [\"no test_parse_datetime references outside docs/ and .git/\", \"README structure table lists _template.py, demo_parse_datetime.py and docs/\", \"CLAUDE.md documents tooling, the template convention and the naming rules\", \"pixi run pre-commit-all shows every hook Passed and none Failed\", \"final acceptance block passes: lint clean, 17 formatted, mypy 17 files, 35 tests\"], \"modelTier\": \"standard\"}\n```"
    }
  ],
  "lastUpdated": "2026-08-15T00:00:00Z"
}
